package com.notes.ui

import android.content.Context
import androidx.appcompat.widget.AppCompatImageView

class NotesImage(context: Context) : AppCompatImageView(context) {
    init {
        //TODO super.XXX
    }
}